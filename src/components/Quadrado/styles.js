import styled from 'styled-components'

export const QuadradoStyled = styled.div`
    width: 50px;
    height: 50px;
    position: absolute;
    top: 0;
    left: 0;
    border-radius: 15px;
    box-shadow: -2px -2px 3px #161616,
            inset -2px -2px 3px #0a0a0a
  ;
  border: 1px solid rgba(0,0,0,0.3);
  opacity: 0.8;
`